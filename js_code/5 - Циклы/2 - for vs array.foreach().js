// Из цикла for мы можем выйти по условию, а forEach будет перебирать каждый элемент


// Внутри for можно написать вложенный for, и это будет лучше читаться чем forEach в forEach


// forEach возвращает нам сам элемент, тогда как for (let i = 0; i < 10; i++) просто возвращает число, которое мы потом подставляем как индекс