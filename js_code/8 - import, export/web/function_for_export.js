const getFullName = (firstName, surname) => firstName + ' ' + surname;
const getFirstLetter = (firstName) => firstName[0];

// export default {fucntionName, functionName, ...}
export default {getFullName, getFirstLetter};